export interface Empregado {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  cpf: string;
  address: string;
  dateOfBirth: string;
  currentlyEmployed: boolean; // Adicionado
}
